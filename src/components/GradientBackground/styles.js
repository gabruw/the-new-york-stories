//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        background: {
            flex: 1,
            display: 'flex'
        }
    });

export default useStyles;
