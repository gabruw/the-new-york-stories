//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        card: {
            padding: 0
        },
        image: {
            minHeight: 400
        }
    });

export default useStyles;
